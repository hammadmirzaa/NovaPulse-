apiVersion: v1
kind: Secret
metadata:
  name: gcp-credentials
  namespace: default
type: Opaque
data:
  # The base64 encoded JSON key file for the service account
  # Generate with: cat key.json | base64 | tr -d '\n'
  key.json: ""
